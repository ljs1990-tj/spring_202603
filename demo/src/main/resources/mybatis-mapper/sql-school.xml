<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.SchoolMapper">
	<!-- 여러개 검색 -->
	<select id="selectProfList" parameterType="hashmap" resultType="com.example.demo.model.Professor">
		SELECT 
			P.*, 
			D2.DNAME AS DNAME2, 
			D1.DNAME AS DNAME3
		FROM PROFESSOR P 
		INNER JOIN DEPARTMENT D1 ON P.DEPTNO = D1.DEPTNO
		INNER JOIN DEPARTMENT D2 ON D1.PART = D2.DEPTNO
	</select>

</mapper>
